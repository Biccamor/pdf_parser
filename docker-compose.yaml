services:
  parser:
    build: . 
    container_name: parser
    ports: 
     - 8010:8010
    restart: unless-stopped
    healthcheck:
      test: ["executable", "arg"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s

volumes:
  - parser