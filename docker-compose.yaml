services:
  parser:  
    build: .
    ports:
      - "8010:8010"
    volumes:
      - .:/parser
      - parser:/root/.cache
    command: uvicorn main:app --host 0.0.0.0 --port 8010 --reload
volumes:
  parser: